{
  "name": "iot-perf-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "npx ts-node load-test.ts",
    "test:light": "npx ts-node load-test.ts --count=100 --concurrency=5",
    "test:heavy": "npx ts-node load-test.ts --count=5000 --concurrency=50"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  }
}
